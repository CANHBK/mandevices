import React from "react";
import {{presentation (pascalCase name)}} from "../views";

const {{controller (pascalCase name)}} = () => {
  return (
    <{{presentation (pascalCase name)}}/>
  );
};

export default {{controller (pascalCase name)}};