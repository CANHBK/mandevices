{
	"scripts": {
		"restart:all": "npm run stop && docker-compose up --build --remove-orphans",
		"restart": "docker-compose up --build --remove-orphans",
		"start": "docker-compose up",
		"stop": "docker-compose stop"
	},
	"devDependencies": {
		"npm-run-all": "^4.1.5"
	}
}
